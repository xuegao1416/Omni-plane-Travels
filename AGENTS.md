# AGENTS.md

## 核心原则

- 这是一个单项目（前端 + 后端 + 数据库），上下文相对稳定。优先在当前仓库内完成工作，不主动扩展到外部资料、无关模块或后续优化。
- 用户当前明确指令 > 本文件规则 > 其他默认偏好。
- 默认用简体中文回复用户；代码、命令、文件路径和技术标识保持英文。
- 先给结论和实际影响，再给必要证据或行动结果。能省略的解释就省略。
- 以正确完成用户当前目标为第一优先级，同时尽量减少无效搜索、重复读取、重复推理、无关修改和过度验证。
- 不为了节省 token、执行时间或改动行数而牺牲正确性、架构一致性或数据安全。

## 项目上下文

- `PROJECT_FUNCTIONAL_SPEC.md` 是项目现有功能、模块关系、数据流和关键架构设计的参考索引。
- 遇到陌生模块、跨模块修改或需要理解整体数据流时，优先在 `PROJECT_FUNCTIONAL_SPEC.md` 中定位相关章节，再按需读取实际代码；不要默认从头通读整个文档。
- 文档用于快速建立项目地图，实际代码是当前实现的最终依据。文档与代码不一致时以代码为准；只有差异影响当前任务时才需要指出。
- 项目存在前端、核心引擎、记忆、变量、世界书、事件/工作流、存档、模块和数据库等跨系统联动。修改其中一处时，只检查与当前改动直接相关的上下游关系，不做无关的全项目审查。
- 保持项目已有的架构原则、状态所有权、数据流和持久化语义。除非当前任务明确要求，不主动替换现有架构或进行大规模重构。

## 自主执行

- 从用户指令和已有上下文推断用户的实际目标与任务范围，倾向于行动并持续推进，直到当前目标得到可检查、可使用的结果。
- 对不会改变需求语义、架构方向、用户数据或最终行为的常规实现细节，自行采用与现有代码最一致的合理方案，不停下来询问用户。
- 只有缺失信息会实质改变需求结果、架构方向，或操作具有破坏性、不可逆性、明显的数据兼容风险时才询问用户。
- 如果存在不依赖用户答复的工作，先继续完成这些工作，不因一个非阻塞问题停止整个任务。
- 可逆、影响小的改动直接执行；破坏性或不可逆操作前确认。
- 用户批准的应该是具体、可检查的结果，而不是空计划。能直接实施时不要反复停留在计划、复述、确认或方案讨论阶段。
- 当用户建议明显不利于目标时直接指出，不迎合。
- 当前目标完成后停止，不主动扩展到相邻问题、潜在优化、新功能或额外重构。

## 搜索与阅读

- 开始修改前，先定位最可能相关的入口、符号、调用链和数据结构。
- 搜索采用“先窄后宽”：优先搜索明确的函数、类型、路径、错误信息和调用点；只有现有证据不足时才扩大范围。
- 已有足够证据能够安全实施时，停止继续搜索并进入修改。
- 不为了“完整理解项目”扫描整个仓库。
- 大文件优先搜索目标符号并读取局部上下文，不默认整文件读取。
- 已读取且未发生变化、当前上下文仍足够的文件不要重复完整读取；需要复核时优先读取具体符号或局部范围。
- 优先利用当前上下文、已经读取的代码和 `PROJECT_FUNCTIONAL_SPEC.md` 中已有的信息。
- 独立的搜索、读取和查询尽量批量执行，避免大量连续的小型工具调用。
- 不收集不会改变当前实现决策的信息。

## 代码与架构

- 修改前快速理解相关模块现有模式、类型、调用关系和状态所有权，并保持一致。
- 优先最小必要改动，避免与当前任务无关的重构、清理、格式化、抽象升级、依赖替换或代码风格调整。
- “最小改动”指能够正确解决问题且保持架构一致性的最小改动；不要为了减少改动行数复制逻辑、绕过已有抽象或制造技术债。
- 前端、后端、数据库及持久化相关改动要检查直接关联的接口、类型、序列化/反序列化和迁移一致性。
- 涉及核心引擎、异步管线、状态同步、存档、回滚、记忆或世界演化时，保留现有作用域守卫、状态所有权、快照、增量持久化和竞态防护语义。
- 涉及数据库或存档格式时，运行时保持单一 canonical schema；当前 Vn 默认只保留直接上一代 V(n-1) → Vn 的内部迁移，更早链路应淘汰。外部协议兼容与数据库 SQL 部署历史单独判断。
- 不修改与当前任务无关的用户代码，即使发现可以顺手优化。
- 临时文件、调试代码和一次性脚本用完立即清理。

## 测试与验证

- 验证范围必须与本次改动的规模、影响范围和风险相称。
- 优先运行最相关、最小范围的测试、类型检查或关键路径验证。
- 对可逆、低影响、实现与现有模式高度一致的小改动，不主动编写没有实际验证价值的新测试。
- 对跨模块数据流、状态同步、存档/回滚、数据库迁移、核心管线等高耦合修改，应验证直接受影响的关键路径，不因节省 token 而跳过必要验证。
- 已有相关检查通过且没有新的具体疑点后，停止扩大或重复测试。
- 只有出现新修改、测试失败、行为异常或尚未解决的具体风险时，才扩大验证范围。
- 验证失败时优先根据失败信息定位原因，不立即扩大为全项目排查。
- 相关代码之后没有变化时，不重复运行已经通过的相同检查。
- 不主动修复与当前任务无关的既有测试失败；确认其与本次修改无关后简要记录即可。
- 收尾时删除本次产生且之后无用的临时文件。

## 推理与效率

- 不把更多搜索、更多推理、更多测试或更大的改动范围视为天然更可靠。
- 优先通过明确任务边界、相关上下文、现有架构和针对性验证解决问题。
- 一旦已有足够证据支持实现决策，就停止探索并执行。
- 一旦必要验证通过且没有新的具体疑点，就停止验证。
- 不重复分析已经得出且没有新证据推翻的结论。
- 不因为存在理论上的其他可能性而穷举所有实现、边缘情况或替代方案。
- 遇到复杂问题时可以扩大分析范围，但扩大范围必须服务于一个尚未解决的具体问题。
- 不为了表现谨慎而增加没有实际决策价值的工作。

## 工具使用

- 搜索文件或文本优先使用高效方式，如 `rg`。
- 在本仓库运行 shell 命令（`git` / `ls` / `grep` / `diff` / `find` 等）时，优先使用 `rtk` 前缀，通过 Rust Token Killer 压缩输出。
- `rtk` 不在 PATH、命令不兼容，或压缩后的输出不足以判断当前问题时，退回原生命令。
- 命令输出只获取当前判断所需范围，避免无目的输出大型日志、完整文件、完整 diff 或整个目录树。
- 独立的读取、搜索和检查尽量批量执行。
- 工具已经提供足够信息时，不使用另一个工具重复获取相同信息。
- 工具调用失败时先根据错误修正调用，不机械重复相同命令。

## 子 Agent

- 简单任务、单一调用链任务以及强共享状态任务由当前 Agent 直接完成。
- 只有任务可以真正独立拆分，并且并行执行能够明显节省时间、减少主 Agent 上下文负担或提高复杂任务质量时才使用子 Agent。
- 不为了“多一个视角”、重复验证或简单文件搜索而启动子 Agent。
- 委派前明确任务范围、必要输入、预期输出和完成标准。
- 子 Agent 只处理被委派的范围，不自行扩展任务。
- 避免多个子 Agent 重复读取和分析同一批文件。
- 主 Agent 负责整合结果、解决冲突，并只对最终实现进行必要验证。

## 沟通与输出

- 工作过程中以执行为主，不逐条播报常规搜索、读取、修改和测试动作。
- 只有出现阻塞、需要用户决策、发现会实质改变方案的重要事实，或完成明显阶段性成果时才同步。
- 使用简洁、连贯的段落；只有信息真正并列、需要比较或存在明确步骤时才使用列表。
- 技术细节只保留对理解结论、判断风险、复现问题或继续工作有帮助的部分。
- 避免套话、重复总结、无意义术语、未经请求的方案对比和冗长过程描述。
- 需要用户决策时给出少量明确选项并说明推荐，不抛宽泛的开放式问题。
- 最终回复简洁说明完成了什么、关键影响、进行了哪些必要验证，以及仍存在的真实问题（如有）。
- 不重复用户已经知道的信息。
- 不在结尾主动提出与当前任务无关的后续优化、重构或新功能。