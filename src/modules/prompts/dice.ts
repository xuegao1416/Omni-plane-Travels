// 骰子检定模块 — Prompt 模板

/** 骰子检定规则（固定，不需要AI生成） */
export const DICE_RULES_PROMPT = `【骰子检定系统】
当玩家进行有风险的行动时，使用骰子检定判定结果。

规则：掷1d20 + 属性修正值 vs 难度等级(DC)

【属性修正值计算】
修正值 = floor((属性值 - 10) / 2)

【难度等级(DC)】
- DC 5:  简单（普通人也能做到）
- DC 10: 普通（需要一定能力）
- DC 15: 困难（需要较高能力）
- DC 20: 极难（需要顶尖能力）
- DC 25: 传奇（几乎不可能）

【特殊骰】
- 自然20（骰出20，不含修正）：大成功，额外有利效果
- 自然1（骰出1，不含修正）：大失败，额外不利效果

【触发骰子检定的格式】
当需要骰子检定时，在叙事中输出以下标记，系统会自动渲染骰子卡片供玩家掷骰：
[DICE_ROLL]{attr:"属性名", dc:难度数字}[/DICE_ROLL]

例如：
- 力量检定：[DICE_ROLL]{attr:"力量", dc:15}[/DICE_ROLL]
- 敏捷检定：[DICE_ROLL]{attr:"敏捷", dc:10}[/DICE_ROLL]
- 使用当前世界的实际属性名称。

在叙事中体现骰子结果的随机性和不确定性。`;

/** 运行时UpdateVariable规则 */
export const DICE_UPDATE_RULES = `【骰子检定更新规则】
骰子检定由玩家在前端卡片中操作，AI无需手动更新骰子数据。`;
